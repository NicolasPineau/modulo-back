{% extends 'base.html.twig' %}

{% block title %}Modifier le scope{% endblock %}

{% block body %}
    <h1>Modifier le scope</h1>

    <div class="container-fluid">
        {{ include('scope/_form.html.twig') }}
    </div>

    <div class="row">
        <div>
            <button class="btn-create" form="form_scope">Modification</button>
        </div>
        <div>
            <a href="{{ path('app_scope_index') }}">Revenir à la liste</a>
        </div>
    </div>
{% endblock %}
