{% extends 'base.html.twig' %}

{% block title %}TypeEvent{% endblock %}

{% block body %}
    <h1>TypeEvent</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ type_event.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_type_event_index') }}">Retour à la liste</a>

    <a href="{{ path('app_type_event_edit', {'id': type_event.id}) }}">Modifier</a>

    {{ include('type_event/_delete_form.html.twig') }}
{% endblock %}
